#!/command/with-contenv bashio
# Ollama must not die in silence: without it the interface stays up but structures nothing
# any more, and the symptom the user sees is only "LLM not ready".
bashio::log.warning "Ollama stopped (code ${1}). s6 restarts it."
